{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix app/web",
    "build": "npm run build --prefix app/web",
    "start": "npm run preview --prefix app/web",
    "lint": "npm run lint --prefix app/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "next-themes": "^0.4.6"
  }
}
